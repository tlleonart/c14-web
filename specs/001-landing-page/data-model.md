# Data Model: Landing Page para Servicios de IA

**Date**: 2026-01-28
**Feature**: 001-landing-page

## Entities

### ContactRequest

Represents a contact form submission from a potential client.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `_id` | Id | Yes | Auto-generated by Convex |
| `_creationTime` | number | Yes | Auto-generated timestamp (ms since epoch) |
| `name` | string | Yes | Full name of the person contacting |
| `email` | string | Yes | Email address for response |
| `message` | string | Yes | Description of their project/needs |
| `status` | string | No | Processing status: "new" \| "read" \| "resolved" |

**Indexes**:
- `by_status`: For filtering by status
- `by_creation_time`: For sorting by date (implicit)

**Validation Rules**:
- `name`: Minimum 2 characters
- `email`: Valid email format
- `message`: Minimum 10 characters

### Service (Static Data)

Services are defined as static data in code, not stored in database.

```typescript
// src/modules/landing/data/services.ts
export interface Service {
  id: string
  name: string
  description: string
  icon: string
  features: string[]
}

export const services: Service[] = [
  {
    id: 'ai-automation',
    name: 'Automatizaciones con IA',
    description: 'Optimización de flujos de trabajo mediante automatización inteligente y orquestación de servicios.',
    icon: 'settings_input_component',
    features: [
      'Pipeline de datos automatizado',
      'Integración de APIs',
      'Flujos de trabajo inteligentes'
    ]
  },
  {
    id: 'ai-development',
    name: 'Desarrollo de IA',
    description: 'Implementación de modelos de lenguaje y agentes autónomos personalizados para tu negocio.',
    icon: 'neurology',
    features: [
      'LLMs personalizados',
      'Agentes autónomos',
      'Integración RAG'
    ]
  },
  {
    id: 'web-development',
    name: 'Desarrollo Web',
    description: 'Interfaces modernas y escalables con foco en experiencia de usuario y rendimiento.',
    icon: 'code_blocks',
    features: [
      'Aplicaciones Web Modernas',
      'Dashboards interactivos',
      'Optimización de rendimiento'
    ]
  },
  {
    id: 'software-development',
    name: 'Desarrollo de Software',
    description: 'Soluciones de software a medida para resolver problemas específicos de tu negocio.',
    icon: 'terminal',
    features: [
      'Arquitectura cloud-native',
      'APIs robustas',
      'Sistemas escalables'
    ]
  }
]
```

## Convex Schema

```typescript
// convex/schema.ts
import { defineSchema, defineTable } from "convex/server"
import { v } from "convex/values"

export default defineSchema({
  contactRequests: defineTable({
    name: v.string(),
    email: v.string(),
    message: v.string(),
    status: v.optional(
      v.union(
        v.literal("new"),
        v.literal("read"),
        v.literal("resolved")
      )
    ),
  })
    .index("by_status", ["status"]),
})
```

## State Transitions

### ContactRequest Status Flow

```
[new] → [read] → [resolved]
  ↑        ↓
  └────────┘ (can go back if needed)
```

| From | To | Trigger |
|------|-----|---------|
| (none) | new | Form submission |
| new | read | Owner views the request |
| read | resolved | Owner responds/closes |
| resolved | read | Reopen if needed |

## Relationships

This feature has no complex relationships. ContactRequest is a standalone entity.

```
┌─────────────────────┐
│   ContactRequest    │
├─────────────────────┤
│ name: string        │
│ email: string       │
│ message: string     │
│ status: string?     │
└─────────────────────┘
```

## Data Access Patterns

### Write Operations

| Operation | Function | Frequency |
|-----------|----------|-----------|
| Create contact | `contacts.create` | On form submit |
| Update status | `contacts.updateStatus` | Manual (future) |

### Read Operations

| Operation | Function | Frequency |
|-----------|----------|-----------|
| List all | `contacts.list` | Admin view (future) |
| List by status | `contacts.listByStatus` | Admin filter (future) |

## Zod Schemas (for API validation)

```typescript
// src/server/api/routers/contact.ts
import { z } from 'zod'

export const contactFormSchema = z.object({
  name: z
    .string()
    .min(2, 'El nombre debe tener al menos 2 caracteres')
    .max(100, 'El nombre es demasiado largo'),
  email: z
    .string()
    .email('Por favor ingresa un email válido'),
  message: z
    .string()
    .min(10, 'El mensaje debe tener al menos 10 caracteres')
    .max(2000, 'El mensaje es demasiado largo'),
})

export type ContactFormData = z.infer<typeof contactFormSchema>
```
